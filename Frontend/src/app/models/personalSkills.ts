import { LanguageElement } from './languageElement';

export interface PersonalSkills {
    languages:Array<LanguageElement>,
    comSkills: Array<String>,
    organisationSkills: Array<String>,
    jobSkills: Array<String>,
    digitalSkills: Array<String>,
}